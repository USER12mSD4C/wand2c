sc.true

extern fn path_exists(u8* path) -> u64;
extern fn path_is_dir(u8* path) -> u64;
extern fn path_join(u8* dest, u8* a, u8* b);
extern fn path_dirname(u8* path, u8* dest);
extern fn path_basename(u8* path) -> u8*;
